// Kaufverhalten
SELECT items.product_id, kunden.customer_id
FROM sales_flat_order_item AS items

INNER JOIN sales_flat_order_grid AS kunden
ON items.order_id = kunden.entity_id

// Warenkoerbe
SELECT order_id, product_id
FROM sales_flat_order_item
ORDER BY order_id, product_id